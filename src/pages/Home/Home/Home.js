import React from 'react';
import { Button } from "keep-react";

const Home = () => {
    return (
        <div>
            <h1>This is home</h1>
            <Button size="md">Default</Button>
        </div>
    );
};

export default Home;